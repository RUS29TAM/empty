import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['images.dog.ceo', 'plus.unsplash.com', 's31vla.storage.yandex.net']
    }
    /* config options here */
};

export default nextConfig;
